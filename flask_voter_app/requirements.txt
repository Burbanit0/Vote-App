# Vote Lab backend — FastAPI (Flask fully retired in Phase 4.5.b).

# ── Web framework (ASGI) ─────────────────────────────────────────────────────
fastapi==0.121.2
uvicorn[standard]==0.40.0
python-socketio==5.13.0        # WebSocket streaming (AsyncServer mounted on FastAPI)
slowapi==0.1.9                 # per-IP rate limiting for the public /api/v1 surface
httpx==0.28.1                  # FastAPI TestClient + httpx-oauth
pydantic==2.13.4
pydantic-settings==2.12.0      # 12-factor env config for api/core/config.py

# ── Auth ─────────────────────────────────────────────────────────────────────
fastapi-users[sqlalchemy]==15.0.5   # JWT issuance + password hashing (pwdlib)
httpx-oauth==0.17.0
google-auth==2.38.0
bcrypt==4.2.1                  # verify legacy $2b$ password hashes

# ── Database (async) ─────────────────────────────────────────────────────────
SQLAlchemy==2.0.41
asyncpg==0.30.0                # Postgres driver (prod)
aiosqlite==0.21.0             # SQLite driver (tests)

# ── Cache ────────────────────────────────────────────────────────────────────
redis==5.2.1

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.1.1
structlog==25.4.0              # JSON structured logging (api/engine/utils/logger.py)

# ── Data / science (the simulation engine) ───────────────────────────────────
numpy==2.2.6
scipy==1.15.3

# ── Dev / test ───────────────────────────────────────────────────────────────
flake8==7.2.0
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==6.1.0
