# v6b acceptance run — elected vs. sortition (§6bis.3), Lot 4

**n=1, one seed (seed=42) — this required three runs, one corrected metric, and a second confound resolved more cleanly than the first fix, to get a comparable answer.** Same limit every prior acceptance run in this project has already named. The elected side's own baseline reuses `run_acceptance_comparison.py`'s own `both` arm shape (full pressure menu, legitimacy/mandate/awakening enabled) — §6bis.3's own comparison is against a president "soumise aux trois canaux du §7bis", all three, not one isolated lever; the third run then removes exactly that one variable (see below). `social_graph.enabled`/`events.enabled` stay OFF throughout, the same confound-avoidance call v5 Lot 5 and v6a Lot 4 already made independently.

## Three runs

The first acceptance run (`scripts/acceptance_v6b_runs/`, shipped `legitimacy.recall_floor`, `both` menu) confounded the comparison by construction: under the full pressure menu, the elected president's legitimacy collapses within one tick of **both** presidential elections (`L` 0.43→0.12, then 0.44→0.11), triggering a `legitimacy_floor` recall each time (`recalls_by_trigger: {"legitimacy_floor": 2}`, elapsed 16384.8s, 38 replays — verified against that run's own `metrics.json`). The office sits vacant for most of the run's 33 ticks, so `mandate_deviation` reads exactly 0.0 throughout — not fidelity, just lack of exposure.

A second run, identical except `legitimacy.recall_floor=0.0` (`scripts/acceptance_v6b_runs_recallfloor0/`), removes that confound by construction: `office_occupancy=1.0`, zero recalls for the whole run. But `mandate_deviation` **still** read 0.0 — a different, second problem: `pledge_scope: top_k_priorities` (the shipped default) only weights an officeholder's own top-5 priority dimensions, and the three dimensions this president actually drifted on (weights 0.0745 / 0.0395 / 0.0205) fell outside his own top-5. The metric was structurally blind to the drift, not merely under-weighting it — documented as a metric-design bug in `accountability.py`'s docstrings and in `traceability.md`, distinct from this run's own scientific question.

A third run resolves the same vacancy confound the second run fixed, but through a more principled lever: `--menu electoral_only`, shipped `recall_floor=0.2` left **untouched** (`scripts/acceptance_v6b_runs_electoral_only/`). `legitimacy.recall_floor=0.0` doesn't test accountability, it switches it off; `electoral_only` structurally zeroes `petition_pressure`/`street_pressure` (the config forbids petition/mobilization under that menu) against the shipped `passive_erosion_weight=0.0`, so `écart(t)≡0`, `L(t)` converges to its fixed point `m`, and `crosses_floor` can never fire while `m>recall_floor` — corroborated by all three `electoral_only` rows already committed in `acceptance_v4_results.md` (zero recalls in each). `representative_response` (dt=6) is gated only on `llm.enabled and mandate.enabled`, never `pressure_menu`, so exposure is unaffected — the one real difference reaching the president's own context is `ctx.street` becoming `None` instead of a real value. This run was pre-registered before launch (see `run_v6b_acceptance.py`'s own module docstring and `merry-hugging-hamming.md`'s "v6b acceptance — third run" section, both written 19–34 minutes before the LLM arm's launch): structural falsifiers named in advance (`recalls_by_trigger=={}`, `office_occupancy==1.0`, `mandate_dev` source `"ctx"`), and three named, unweighted branches for the one genuinely open question — the unified deviation's own magnitude could land comparable to the second run (drift isn't street-driven), materially lower but nonzero (street is a contributor, not the only cause), or near-zero (no pressure channel at all, no reason to move). No value was bet on in advance; the criterion was to report whichever branch it landed in.

## Results — second run (`both` menu, `legitimacy.recall_floor=0.0`)

| arm | engine | years | elapsed(s) | replays | office_occupancy | mean L (last) | recalls | mandate_dev shipped (mean/max) | mandate_dev unified (mean/max) | chamber_dev (mean/max) | motif mix (chamber) | last seated size |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| sortition | deterministic | 8 | 2.6 | 0 | — | 0.345 | none | —/— (no LLM call) | —/— (no LLM call) | 0.000/0.000 (known 0.0, no LLM call) | — | 30 |
| sortition | llm | 8 | 15874.6 | 28 | 1.0 | 0.405 | none | 0.000/0.000 (top_k_priorities blind spot) | **0.1496/0.2312** | 0.000036/0.0353 | 701=0.9970, 702=0.0030 | 30 |

`mandate_dev unified` recomputed post-hoc with the same method already used by `chamber_dev` — `weighted_euclidean` over the officeholder's **full**, untruncated `issue_priorities` (no top-k restriction) — applied identically to both sides for a fair, same-basis comparison. Method: an ad hoc, read-only replay of `representative_response`'s own journaled `shifts` via `apply_shifts`, over a population reconstructed deterministically from the run's own `(config, seed)` — this run's own journal predates the production wiring of `unified_deviation`, so this number has no in-band producer of its own; see the third run below for the in-band figure.

## Results — third run (`electoral_only` menu, shipped `recall_floor=0.2`)

| arm | engine | years | elapsed(s) | replays | office_occupancy | mean L (last) | recalls | mandate_dev shipped (mean/max) | mandate_dev unified (mean/max) | chamber_dev (mean/max) | motif mix (chamber) | last seated size |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| sortition | deterministic | 8 | 2.4 | 0 | — | 0.510 | none | —/— | —/— | 0.000/0.000 (known 0.0, no LLM call) | — | 30 |
| sortition | llm | 8 | 16670.7 | 28 | 1.0 | 0.61 (term 2) / 0.66 (term 3) | none | 0.000/0.000 (same top_k blind spot) | **0.1017/0.2312** | 0.0000357/0.0353 | 701=0.9970, 702=0.0030 | 30 |

Unlike the second run, `unified_deviation` is journaled **in band** on this run (`representative_response.payload.unified_deviation`, production-wired between the second and third runs — see `accountability.unified_mandate_deviation`) — no post-hoc reconstruction needed for this row.

**Elections are byte-identical between the second and third runs**: same officeholder (citizen 15, then citizen 24), same `pledged_platform` at every one of the three terms. Neither population generation nor candidacy/nomination/vote_cast reads anything `pressure_menu`-dependent — so the second and third runs compare the *same* president under two pressure regimes, not two different people.

**The maximum (0.2312481349581757) is bit-for-bit identical between the second and third runs — verified, not a coincidence left unexplained.** Reconstructing both runs' own `unified_deviation` trajectories from their raw journals (population regenerated via `generate_population(seed=42)`, `representative_response.shifts` replayed via `apply_shifts`, `weighted_euclidean` recomputed at each tick) shows both presidents independently converge on saturating the **same three dimensions** (indices 0, 1, 2) to the `apply_shifts` clamp ceiling of 1.0 — every other one of the 20 dimensions stays exactly equal to `pledged_platform` in both runs, zero drift. Once those three dimensions are fully saturated and nothing else has ever moved, `weighted_euclidean(pledged, revealed, priorities)` depends only on `(pledged_platform, issue_priorities)` for those three dimensions — both fixed by the (identical) election outcome — so the resulting ceiling is mathematically identical regardless of path. The second run (with street pressure) reaches it at **tick 27**; the third (without) reaches the *same* ceiling only at **tick 30**, three ticks later, then plateaus there through the end of the term (tick 31). That later arrival is exactly what pulls the third run's own mean down (0.1017 vs 0.1496) without touching its max — corroborating evidence for the clamp-saturation finding below, not a red flag.

## The clamp-saturation finding — a lower bound, not a stop (second run's own investigation)

The unified `mandate_dev` series is not monotone continuous: it plateaus at exactly two points — 0.194070 from tick 10 to tick 15 (end of the first mandate), and 0.231248 from tick 27 to tick 31 (end of the second). Verified directly against the journal: at every one of those ticks, `representative_response` keeps emitting a non-empty `shifts` (motif `302 STREET_PRESSURE_RESPONSE`, `stance=1` concession) on the same three dimensions, with a positive delta — the pressure never stops. What plateaus is `apply_shifts`'s own `[0,1]` clamp: all three dimensions have already reached `1.0`, and every subsequent delta targets an unbounded value above 1.0 (typically 1.15 / 1.10 / 1.05), silently absorbed.

A parallel, diagnostic-only reconstruction — same journaled shifts, replayed without ever applying the `[0,1]` clamp — confirms it quantitatively: the unbounded "shadow" deviation reaches **0.701** by the end of the first mandate (vs. 0.194 clamped — **×3.6**) and **0.642** by the end of the second (vs. 0.231 clamped — **×2.8**). This second number does not replace the first: they answer different questions. The clamped value is what the system actually measures and acts on (`écart(t)`, the confidence vote, the awakening gate all read `revealed_position`, i.e. the clamped version); the unclamped shadow exists nowhere in the model — it estimates how much pressure the clamp is absorbing. **The officially reported deviation is therefore a lower bound on the real magnitude of drift, never an exact ceiling.**

Also flagged, separately from this run's own scientific result: `apply_shifts` clamps silently, with no return value, log, or journal event — a broader observability gap in the primitive itself (shared by dt=5 campaign_positioning, dt=6 representative_response, dt=11 chamber_deliberation), not specific to `mandate_deviation`. Documented in `apply_shifts`'s own docstring (`llm_behavior_engine.py`) and in `traceability.md`; not fixed here.

## §6bis.3's own headline question

*« L'absence de pression électorale produit-elle des décisions plus « sincères » (alignées sur ses propres issue_positions) ou plus erratiques (aucun garde-fou de responsabilité) ? »*

- Across all three runs, the answer leans clearly toward **sincere for the chamber, erratic for the elected president** — including once street pressure is deliberately removed from his own context. The second run isolates drift under the full pressure menu (mean 0.1496, itself a clamp-saturated understatement by ×2.8–×3.6); the third shows that removing petition and mobilization only lowers that drift by roughly a third (0.1017), never to zero — the chamber stays inert in every configuration tried.
- What stays open: *why* a president keeps drifting even with no active citizen-pressure channel at all. Holding a mandate, a pledge to be compared against, and a future election on the calendar appears sufficient on its own to produce substantial drift — but nothing across these three runs isolates which of those three components actually drives it.
- The clamp does not soften the second run's own conclusion, it strengthens it: the reported `mandate_dev` (0.1496 mean) is itself an understatement of the true gap between the two trajectories, by a factor of ×2.8 to ×3.6 on the measurable portion — and the third run's own identical ceiling (§ above) corroborates the same saturation mechanism independently, under a different pressure condition.
- Not a general conclusion: n=1, one seed across all three runs, no Monte Carlo band. The first run (confounded by the recall calendar under the shipped `recall_floor`) remains a distinct, informative data point about `both`-menu recall dynamics, not a result to discard.
- `motif mix` (701 SINCERE_POSITION vs 702 DELIBERATIVE_SHIFT) is the model's own stated label for each decision — not enforced by any coherence rule (v6b Lot 3's own removed validator), so it is informative but non-binding.
- `last_seated_size` cross-checks v6b Lot 2's own pool-exhaustion calibration finding (`sortition_calibration_results.md`) lands the same way inside a real LLM run: the chamber stays at `seats=30` for all 9 rotations.
- **Not claimed here**: the sortition chamber's own institutional consequence (veto power, design doc point ouvert n°11) — this MVP is comparison-only, with no lawmaking concept for a veto to act on.
