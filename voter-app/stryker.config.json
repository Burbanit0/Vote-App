{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing for the client voting engine — the frontend half of what mutmut does for fast_api_voter (.github/workflows/mutation-testing.yml runs both). Scoped to src/lib/playgroundVoting.ts: it carries the project's thesis (the rule decides the winner) and is one of the two files locked by the engine-parity harness. Non-blocking until two real runs give us a defensible thresholds.break — see the plan's chantier 4b.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": { "configFile": "vitest.stryker.config.ts" },
  "mutate": ["src/lib/playgroundVoting.ts"],
  "coverageAnalysis": "perTest",
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "_thresholds_comment": "break is set just UNDER the measured score, to lock in what exists rather than to aspire. Measured 2026-08-24: 80.82% total (1092 killed, 113 timeout, 277 survived, 9 uncovered) over a 38-minute run. The 4-point gap absorbs timeout variance, which is the only non-deterministic part of a Stryker pass. Target 85 once the spatial-helper and tie-break chantiers land.",
  "thresholds": { "high": 85, "low": 70, "break": 76 },
  "timeoutMS": 60000,
  "tempDirName": ".stryker-tmp"
}
