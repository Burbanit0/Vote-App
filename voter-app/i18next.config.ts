import { defineConfig } from 'i18next-cli';

export default defineConfig({
  locales: ['fr', 'en'],
  extract: {
    // Test files aren't user-facing UI text — excluded, same principle as
    // every other "hardcoded string" scan in this repo only caring about
    // what a real visitor sees. Extglob, not a `!`-prefixed second pattern:
    // this tool's underlying `glob()` call (linter.ts) takes no `ignore`
    // option and doesn't treat `!`-prefixed array entries as excludes (that
    // changed in glob v9+, verified directly — a leading-`!` pattern mixed
    // into the same array is simply matched literally, not subtracted).
    input: ['src/**/!(*.test|*.d).{ts,tsx}'],
    output: 'src/i18n/locales/{{namespace}}.{{language}}.ts',
    defaultNS: 'translation',
    outputFormat: 'ts',
    ignoredAttributes: [
      'data-testid',
      'data-tour',
      'data-tab',
      'id',
      'name',
      'type',
      'variant',
      'size',
      'role',
      'href',
      'style',
      'className',
      'key',
      'htmlFor',
      'as',
      'align',
      'side',
      'placement',
      'to',
      'testId',
      'colorScheme',
      // SVG presentation attributes — this app is SVG-native (maps, charts,
      // custom visualizations, see CLAUDE.md's voter-ui skill), so these
      // outnumber every other false-positive category combined. None of
      // them are user-facing text.
      'fill',
      'stroke',
      'strokeWidth',
      'strokeDasharray',
      'strokeLinecap',
      'strokeLinejoin',
      'strokeOpacity',
      'fillOpacity',
      'textAnchor',
      'dominantBaseline',
      'viewBox',
      'preserveAspectRatio',
      'transform',
      'points',
      'd',
      'cx',
      'cy',
      'r',
      'rx',
      'ry',
      'x',
      'y',
      'x1',
      'x2',
      'y1',
      'y2',
      'width',
      'height',
      'gradientUnits',
      'gradientTransform',
      'offset',
      'stopColor',
      'stopOpacity',
      'markerEnd',
      'markerStart',
      'markerWidth',
      'markerHeight',
      'orient',
      'clipPath',
      'clipRule',
      'patternUnits',
      'spreadMethod',
      'shapeRendering',
      'vectorEffect',
      'aria-live',
    ],
  },
});
