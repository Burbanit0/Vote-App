{
  "name": "vote-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev orchestrator — run the FastAPI backend and the Vite frontend together.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "python -m uvicorn api.main:app --port 4434 --reload --app-dir flask_voter_app --reload-dir flask_voter_app",
    "dev:frontend": "npm --prefix voter-app start",
    "setup": "npm --prefix voter-app install && python -m pip install -r flask_voter_app/requirements.txt"
  }
}
